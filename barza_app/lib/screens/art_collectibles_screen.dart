import 'package:flutter/material.dart';

class ArtCollectiblesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Art & Collectibles'),
      ),
      body: Center(
        child: Text('Art & Collectibles Category'),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class GymEquipmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gym Equipment'),
      ),
      body: Center(
        child: Text('Gym Equipment Category'),
      ),
    );
  }
}

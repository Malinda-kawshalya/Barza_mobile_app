import 'package:flutter/material.dart';

class SoftwareLicensesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Software Licenses'),
      ),
      body: Center(
        child: Text('Software Licenses Category'),
      ),
    );
  }
}
